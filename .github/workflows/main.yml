on: [push]

jobs:
  pandoc-job:
    runs-on: ubuntu-latest
    name: a job to pandoc
    steps:
      - name: pandoc action step
        uses: Amusac/markdown-to-pdf-ja@0.1
        id: pandoc
        with:
          input-file: "input.md"
